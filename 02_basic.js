alert("hello")
alert(3+3) // only give o/p when use in browser not in node


//number    bigint   string    boolean    null => standalone value (EX.  when we want temp but temp not available then we cann't fetch 0 we fetch null )
// undefine => till value not assign      symbol=> unique

//objects

console.log(typeof(null))
console.log(typeof(undefined))
console.log(typeof(let))
